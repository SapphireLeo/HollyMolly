types = ('strawberry', 'banana', 'lime', 'plum')    # tuple of string
number = (5, 3, 3, 2, 1)                # tuple of positive integer
min_member = 2                          # positive integer, must be bigger than 1
max_member = 6                          # positive integer

total_card=len(types)*sum(number)       # total number of card

# this must be a positive integer.
number_for_valid_bell=5                 # positive integer

# this must be a positive in
bell_penalty = 1                        # positive integer, must be smaller than total number of card.
